# MongoDB Configuration
# Using MongoDB Driver (Data API is deprecated)

# MongoDB Connection URI
# Used by: Admin UI backend, setup scripts, and Worker (via Admin API)
# Get from MongoDB Atlas: Connect → Connect your application → Node.js
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority"

# Database name
MONGODB_DATABASE=toran

# Admin API URL (Worker fetches configs from here)
# Local dev: http://localhost:5173
# Production: will be set via wrangler secret
ADMIN_API_URL=http://localhost:5173
