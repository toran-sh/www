# MongoDB Configuration
# Using MongoDB Driver (Data API is deprecated)

# MongoDB Connection URI
# Used by: Admin UI backend, setup scripts
# Get from MongoDB Atlas: Connect → Connect your application → Node.js
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority"

# Database name
MONGODB_DATABASE=toran

# Email Service (Resend)
# Required for magic link authentication
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Only set this if you need to override the automatic domain detection
# APP_URL=https://your-domain.vercel.app

# Redis Configuration
# Required for gateway config storage
# Format: redis://[[username:]password@]host[:port][/db-number]
# Examples:
#   Local: redis://localhost:6379
#   Upstash: redis://:password@region.upstash.io:port
#   Redis Cloud: redis://user:password@host:port
REDIS_URL=redis://localhost:6379
